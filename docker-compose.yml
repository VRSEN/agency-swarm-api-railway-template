version: '3'

services:
  api-service:
    image: agency-swarm
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - Backend/.env
    restart: always
